- name: Undo Push
  # You may pin to the exact commit or the version.
  # uses: exions/undo-push@1ccaffaa740d650cb8f1f30cbada9205c2785f24
  uses: exions/undo-push@v1.0.0
  with:
    # The branch to undo last commit
    branch: # optional
